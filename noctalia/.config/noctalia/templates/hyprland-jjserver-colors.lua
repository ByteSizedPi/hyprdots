-- Generated from palettes/<name>.json by scripts/server-theme/deploy.sh.
-- Client-side colors for the jjserver special workspace (Hyprland). Consumed by
-- hyprland/.config/hypr/modules/scratchpads.lua so the border matches the cobalt
-- theme rather than the wallpaper palette.
return {
	primary = "{{colors.primary.default.hex_stripped}}",
}
