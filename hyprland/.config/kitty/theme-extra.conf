# theme-overlay: none
# GENERATED by scripts/desktop-theme/apply.sh — the active theme ships no kitty
# settings, so kitty falls back to its own config. Add themes/<name>/apps/kitty.conf
# to give a theme its own, or edit this file and run:
#   scripts/desktop-theme/save.sh <name> --force
