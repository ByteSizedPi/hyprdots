# theme: comicmono

Saved 2026-07-27. Describe the intent here — the key list won't.

- **Feel:**
- **Palette:** derived from the wallpaper — `theme.source = "wallpaper"`,
  `theme.wallpaper_scheme = "faithful"`, `theme.mode = "dark"`. (`theme.builtin =
  "Noctalia"` and `theme.community_palette = "Ayu Green"` are stored but inert
  while `source` is `wallpaper`.)
- **Wallpaper:** `Catppuccin Macchiato/wp4893878.jpg` on DP-1 / HDMI-A-1 / eDP-1
  and as the default; the nested-session output WAYLAND-1 keeps
  `Saves/003_Ringed_kingfisher…jpg`. Paths only — the images live in
  `~/Pictures/Wallpapers/`, not in this repo, so the theme needs them present.
